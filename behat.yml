default:
  suites:
    Wallet:
      paths:
        - %paths.base%/features/Wallet
      contexts:
        - WalletContext
    GPW:
      paths:
        - %paths.base%/features/GPW
      contexts:
        - GPWContext